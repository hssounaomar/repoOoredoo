/* import { RECEIVE_EQUIPEMENT } from '../actions/equipements.actions';

export default function equipementReducer(state = {}, action) {  //1
  switch (action.type) {
    case RECEIVE_EQUIPEMENT:                                     //2
      return action.payload;
    default:
      return state;
  }
}; */